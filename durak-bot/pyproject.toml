[project]
name = "durak-bot"
version = "0.1.0"
description = "Durak bot trained via Reinforcement Learning"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.6",
    "typer>=0.24.1",
]
