[project]
name = "cli"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "durak",
    "dqn",
    "typer",
]

[project.scripts]
game = "cli.main:app"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cli"]